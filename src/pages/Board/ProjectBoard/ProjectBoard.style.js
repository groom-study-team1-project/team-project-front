import React from 'react';
import styled from "styled-components"; import { PostCardWrapper } from "../Board.style";  export const ProjectPostCardWrapper = styled(PostCardWrapper)`   grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); `;
